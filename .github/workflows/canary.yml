# Temporary diagnostic canary — zero third-party actions, zero uses:.
# Distinguishes repo-level startup_failure from workflow-content causes.
# DELETE after diagnosis; must never merge to main.
name: canary
on:
  pull_request:
permissions:
  contents: read
jobs:
  canary:
    runs-on: ubuntu-latest
    steps:
      - run: echo "canary alive"
